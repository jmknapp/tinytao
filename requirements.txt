# RTX 50-series (sm_120) needs a CUDA 12.8+ wheel, not the default cu126 build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# then the rest of this file from PyPI.
torch>=2.7
numpy>=1.26
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
scikit-learn>=1.4
scipy>=1.12
